// @ts-check
/**
 * DEPRECATED — replaced by `tests/walkthrough.spec.js` Step 1 ("Open
 * homepage") and Step 7 ("Final hygiene: console / page errors").
 *
 * This file is kept only so `git history` continuity is preserved. It
 * intentionally registers zero tests, so Playwright runs through it
 * instantly with no output.
 *
 * If you need the old behaviour, see `tests/walkthrough.spec.js`.
 */
const { test } = require('@playwright/test');
test.skip('moved to walkthrough.spec.js', () => {});
